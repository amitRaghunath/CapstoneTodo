import { ClickEventPropogationDirective } from './click-event-propogation.directive';

describe('ClickEventPropogationDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickEventPropogationDirective();
    expect(directive).toBeTruthy();
  });
});
